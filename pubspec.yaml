name: time_track
description: "trackr_ — intentional time tracking with session notes and music context."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive_flutter: ^1.1.0
  uuid: ^4.5.1
  intl: ^0.20.0
  window_manager: ^0.4.3
  google_fonts: ^6.2.1
  flutter_quill: ^11.5.0
  hotkey_manager: ^0.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
